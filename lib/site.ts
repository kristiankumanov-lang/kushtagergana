/**
 * Canonical production domain — used as the fallback when
 * NEXT_PUBLIC_SITE_URL isn't set (e.g. a local build with no env file).
 * Production has its own NEXT_PUBLIC_SITE_URL and doesn't rely on this.
 */
export const SITE_URL_FALLBACK = "https://www.gerganaguesthouse.com";
